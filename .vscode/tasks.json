{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build",
            "type": "docker",
            "command": "build",
            "args": [
				"streamlit_app.py:latest",
				"${workspaceFolder}/Dockerfile",
				"${workspaceFolder}",
				"."
			]
		},
		{
			"label": "run: debug",
			"type": "docker",
			"command": "run",
			"dependsOn": [
				"docker-build"
			],
			
		}
	]
}